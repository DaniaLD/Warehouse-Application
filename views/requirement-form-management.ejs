<!doctype html>
<html lang="en">
<head>
    <% include ./partials/head.ejs %>
    <title>مدیریت فرم های اعلام نیاز</title>
</head>
<body>
<div class="container">
    <div class="row" style="margin-top: 20px;">
        <h1 style="margin-right: 20px;text-align: right;" class="col-lg-10">پنل کاربری کارشناس انبار > مدیریت فرم های اعلام نیاز</h1>
        <a class="nav-item nav-link" href="/logout" style="float: left;">
            <button class="btn btn-danger"><i class="fa fa-sign-out" aria-hidden="true" style="margin-left: 10px;"></i>خروج</button>
        </a>
    </div>
    <hr>
    <% if(error_msg.length) { %>
    <div class="row alert alert-danger" style="text-align: center;">
        <h5 class="col-lg-12"><%= error_msg %></h5>
    </div>
    <% } %>
    <% if(success_msg.length) { %>
    <div class="row alert alert-success" style="text-align: center;">
        <h5 class="col-lg-12"><%= success_msg %></h5>
    </div>
    <% } %>
    <div class="row">
        <a href="/register-requirement-form" style="margin: 30px;float: right;">
            <button class="btn btn-success"><i class="fa fa-file-text-o" aria-hidden="true" style="margin-left: 10px;"></i>ثبت فرم اعلام نیاز</button>
        </a>
    </div>
    <div class="row">
        <p style="margin: 10px 25px 35px 0px;">
            <i class="fa fa-exclamation-triangle" aria-hidden="true" style="color: red;"></i>
        <span>توجه : کالاهایی که در لیست زیر مشاهده می نمایید، در وضعیت انجام نشده قرار دارند.</span>
        </p>
    </div>
    <table class="table table-striped table-responsive-lg">
        <thead>
        <tr>
            <th style="width: 8%;text-align: center;" scope="col">کد درخواست</th>
            <th style="width: 8%;text-align: center;" scope="col">کد کالا</th>
            <th style="width: 15%;text-align: center;" scope="col">نام کالا</th>
            <th style="width: 15%;text-align: center;" scope="col">واحد</th>
            <th style="width: 15%;text-align: center;" scope="col">تعداد</th>
            <th style="width: 15%;text-align: center;" scope="col">توضیحات</th>
        </tr>
        </thead>
        <tbody>
        <% for(let row of rows) { %>
        <tr">
            <td style="width: 10%;text-align: center;"><%= row.form_id %></td>
            <td style="width: 10%;text-align: center;"><%= row.good_id %></td>
            <td style="width: 20%;text-align: center;"><%= row.name %></td>
            <td style="width: 20%;text-align: center;"><%= row.unit %></td>
            <td style="width: 20%;text-align: center;"><%= row.no %></td>
            <td style="width: 20%;text-align: center;"><%= row.comment %></td>
        </tr>
        <% } %>
        </tbody>
    </table>
</div>

<% include ./partials/scripts.ejs %>
</body>
</html>