<!doctype html>
<html lang="en">
<head>
    <% include ./partials/head.ejs %>
    <title>مدیریت کاربران</title>
</head>
<body>
<div class="container">
    <div class="row" style="margin-top: 20px;">
        <h1 style="margin-right: 20px;text-align: right;" class="col-lg-12">پنل کاربری راهبر > مدیریت کاربران</h1>
    </div>
    <hr>
    <% if(error_msg.length) { %>
    <div class="row alert alert-danger" style="text-align: center;">
        <h5 class="col-lg-12"><%= error_msg %></h5>
    </div>
    <% } %>
    <% if(success_msg.length) { %>
    <div class="row alert alert-success" style="text-align: center;">
        <h5 class="col-lg-12"><%= success_msg %></h5>
    </div>
    <% } %>
    <a href="/add-staff">
        <button class="btn btn-success" style="margin: 30px;float: right;"><i class="fa fa-user-plus" aria-hidden="true" style="margin-left: 10px;"></i>افزودن کارمند</button>
    </a>
    <a href="/edit-staff">
        <button class="btn btn-warning" style="margin: 30px;float: right;"><i class="fa fa-pencil" aria-hidden="true" style="margin-left: 10px;"></i>ویرایش اطلاعات</button>
    </a>
    <table class="table table-striped table-responsive-lg">
        <thead>
        <tr>
            <th style="width: 25%;text-align: center;" scope="col">نام</th>
            <th style="width: 25%;text-align: center;" scope="col">نام خانوادگی</th>
            <th style="width: 25%;text-align: center;" scope="col">سمت</th>
            <th style="width: 25%;text-align: center;" scope="col">وضعیت فعالیت</th>
        </tr>
        </thead>
        <tbody>
        <% for(let staff of staffs) { %>
        <tr>
            <td style="width: 25%;text-align: center;"><%= staff.first_name %></td>
            <td style="width: 25%;text-align: center;"><%= staff.last_name %></td>
            <td style="width: 25%;text-align: center;"><%= staff.post %></td>
            <% if(staff.activate === 1) { %>
            <td style="width: 25%;text-align: center;">فعال</td>
            <% } else { %>
            <td class="table-danger" style="width: 25%;text-align: center;">غیر فعال</td>
            <% } %>
        </tr>
        <% } %>
        </tbody>
    </table>

    <% include ./partials/footer.ejs %>
</div>

<% include ./partials/scripts.ejs %>
</body>
</html>